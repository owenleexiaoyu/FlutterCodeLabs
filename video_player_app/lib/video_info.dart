import 'package:flutter/material.dart';

class VideoInfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Video Info"),
      ),
      body: Center(
        child: Text("Video info page"),
      ),
    );
  }
}
